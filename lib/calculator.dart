import 'package:flutter/material.dart';

class Cal{
  String  type; // ? this to make it a nullabe or avoid saftey measured in dart.
  String ? image;

  Cal({required this.type,required this.image});






}